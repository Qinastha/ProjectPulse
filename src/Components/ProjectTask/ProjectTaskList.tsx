// import React from "react";
// import {CurrentProjectList, ITasks} from "../../core";
//
// interface ProjectTaskListProps {
//     list: CurrentProjectList[]
// }
//
// export const ProjectTaskList: React.FC<ProjectTaskListProps> = ({list}) => {
//     return (
//         <div className="container-taskList">
//             {list.map((id) => (
//                 <div key={list.id} className="task">
//                     {tasks.map((task: ITasks) => {
//                         return task.listId === id ? <p>{task.title}</p> : null;
//                     })}
//                 </div>
//             ))}
//             <button className="task-button">Add Task</button>
//         </div>
//     );
// };

export {};
